# Restrictions

- I am working on other tasks and cannot be disturbed.
- Run automatically; only stop when blocked.

# Setup
1. `npm install`
2. Smoke-test OpenAI (chat + image size), Google Maps, Open-Meteo, Tripadvisor (`category=RESTAURANT`), Pandorium (`birthData` natal chart)
3. Write `.env.local` per `2.tech-stack.md` (Environment)
4. Implement all features per `1.requirement.md` + pitfalls in `2.tech-stack.md`
5. `npm run smoke` then `npm run dev` → http://localhost:3000
